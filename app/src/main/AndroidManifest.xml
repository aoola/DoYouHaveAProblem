<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bridgetech2.aoola.edu.designapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ElecActivity" />
        <activity android:name=".ShebotssActivity" />
        <activity android:name=".SbakkActivity" />
        <activity android:name=".AlectronActivity" />
        <activity android:name=".MidActivity" />
        <activity android:name=".CarActivity" />
        <activity android:name=".info1" />
        <activity android:name=".info2" />
        <activity android:name=".info3" />
        <activity android:name=".elc1"></activity>
    </application>

</manifest>